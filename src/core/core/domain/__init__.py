"""Core domain types — firmware-agnostic CNC concepts.

These types form the contract between the Gateway (publisher) and all
consumers.  They must not reference any specific firmware implementation.
"""
